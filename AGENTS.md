# AGENTS.md

## Git workflow

- Do not create new branches unless explicitly asked.
- By default, make changes directly on `master`.
- This repository does not currently have a GitHub remote/repository unless one is explicitly added.
