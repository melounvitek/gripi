# Follow-up

- Add full SSR/live DOM-tree parity fixtures once the frontend has a lightweight test DOM available. Current regression coverage compares parser semantics and the shared SSR semantic contract for assistant text, thinking, tools, subagents, images, and fingerprints without adding a production dependency or bundler.
